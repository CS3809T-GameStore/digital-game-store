<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" 
      th:with="dir=${@localeContextHolder.getLocale().getLanguage() == 'ar' ? 'rtl' : 'ltr'}"
      th:dir="${dir}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Digital Game Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .navbar-brand {
            font-weight: bold;
            color: #ff0000 !important;
        }
        /* Added rounded bottom corners */
        .navbar {
            border-radius: 0 0 30px 30px;
        }
        .language-flag {
            width: 20px;
            height: 15px;
            margin-right: 5px;
        }
        .currency-symbol {
            font-weight: bold;
            margin-right: 5px;
        }
        .nav-item.dropdown:hover .dropdown-menu {
            display: block;
        }
        .rtl {
            text-align: right;
        }
        .rtl .dropdown-menu {
            text-align: right;
        }
        .rtl .me-2 {
            margin-right: 0 !important;
            margin-left: 0.5rem !important;
        }
    </style>
</head>
<body th:classappend="${@localeContextHolder.getLocale().getLanguage() == 'ar' ? 'rtl' : ''}">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <!-- Brand -->
            <a class="navbar-brand" th:href="@{/}">
                <i class="fas fa-gamepad me-2"></i>
                <span th:text="#{navbar.brand}">Digital Game Store</span>
            </a>

            <!-- Mobile Toggle -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Navbar Content -->
            <div class="collapse navbar-collapse" id="navbarContent">
                <!-- Left Side - Navigation Links -->
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" th:href="@{/}" th:text="#{navbar.home}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" th:href="@{/products}" th:text="#{navbar.products}">Games</a>
                    </li>
                    <li class="nav-item" th:if="${#authorization.expression('isAuthenticated()')}">
                        <a class="nav-link" th:href="@{/library}" th:text="#{navbar.library}">My Library</a>
                    </li>
                    <li class="nav-item" sec:authorize="hasRole('ADMIN')">
                        <a class="nav-link" th:href="@{/admin}" th:text="#{navbar.admin}">Admin Panel</a>
                    </li>
                </ul>

                <!-- Right Side - Settings & Auth -->
                <ul class="navbar-nav ms-auto">
                    <!-- Language & Currency Dropdowns -->
                    <li class="nav-item dropdown me-2">
                        <a class="nav-link dropdown-toggle" href="#" id="languageDropdown" role="button" 
                           data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-globe me-1"></i>
                            <span th:text="#{navbar.language}">Language</span>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="languageDropdown">
                            <li>
                                <a class="dropdown-item" th:href="@{/(lang=en)}">
                                    <img th:src="@{/images/us-flag.png}" alt="English" class="language-flag">
                                    English
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" th:href="@{/(lang=ar)}">
                                    <img th:src="@{/images/sa-flag.png}" alt="Arabic" class="language-flag">
                                    العربية
                                </a>
                            </li>
                        </ul>
                    </li>

                    <li class="nav-item dropdown me-2">
                        <a class="nav-link dropdown-toggle" href="#" id="currencyDropdown" role="button" 
                           data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-money-bill-wave me-1"></i>
                            <span th:text="#{navbar.currency}">Currency</span>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="currencyDropdown">
                            <li>
                                <a class="dropdown-item" th:href="@{/(currency=USD)}">
                                    <span class="currency-symbol">$</span> USD
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" th:href="@{/(currency=SAR)}">
                                    <span class="currency-symbol">ر.س</span> SAR
                                </a>
                            </li>
                        </ul>
                    </li>

                    <!-- Shopping Cart -->
                    <li class="nav-item me-2" th:if="${#authorization.expression('isAuthenticated()')}">
                        <a class="nav-link position-relative" th:href="@{/cart}">
                            <i class="fas fa-shopping-cart"></i>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"
                                  th:text="${session.cart != null ? session.cart.items.size() : 0}">0</span>
                        </a>
                    </li>

                    <!-- User Authentication -->
                    <li class="nav-item dropdown" sec:authorize="!isAuthenticated()">
                        <a class="nav-link dropdown-toggle" href="#" id="authDropdown" role="button" 
                           data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-user me-1"></i>
                            <span th:text="#{navbar.account}">Account</span>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="authDropdown">
                            <li><a class="dropdown-item" th:href="@{/login}" th:text="#{navbar.login}">Login</a></li>
                            <li><a class="dropdown-item" th:href="@{/register}" th:text="#{navbar.register}">Register</a></li>
                        </ul>
                    </li>

                    <!-- User Menu (when logged in) -->
                    <li class="nav-item dropdown" sec:authorize="isAuthenticated()">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" 
                           data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-user-circle me-1"></i>
                            <span sec:authentication="name">User</span>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="userDropdown">
                            <li><a class="dropdown-item" th:href="@{/profile}" th:text="#{navbar.profile}">Profile</a></li>
                            <li><a class="dropdown-item" th:href="@{/library}" th:text="#{navbar.library}">My Library</a></li>
                            <li><a class="dropdown-item" th:href="@{/wishlist}" th:text="#{navbar.wishlist}">Wishlist</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form th:action="@{/logout}" method="post" class="d-inline">
                                    <button class="dropdown-item" type="submit" th:text="#{navbar.logout}">Logout</button>
                                </form>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container mt-4">
        <div th:replace="${content}"></div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
